<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:modelview="clr-namespace:PokePiplup.ModelView" x:DataType="modelview:ListViewModel"
             x:Class="PokePiplup.Page.ListPage">

    <CollectionView ItemsSource="{Binding ListeofPokemon}">
		<CollectionView.Header>
			<Label Text="Liste des pokemons"
			       TextColor="Black"
			       VerticalOptions="CenterAndExpand" 
			       HorizontalOptions="CenterAndExpand"/>
		</CollectionView.Header>
		<CollectionView.ItemTemplate>
			<DataTemplate>
				<Label Text="{Binding Nom}"
				       TextColor="Black"
				       VerticalOptions="CenterAndExpand" 
				       HorizontalOptions="CenterAndExpand"/>
			</DataTemplate>
		</CollectionView.ItemTemplate>
	</CollectionView>
</ContentPage>